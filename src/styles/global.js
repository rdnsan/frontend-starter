const GlobalStyle = `
  *,
  ::before,
  ::after {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  body {
    background-color: ghostwhite;
  }

  h1 {
    text-align: center;
    padding-top: 2rem;
  }
`;

export default GlobalStyle;
